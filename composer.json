{
    "config": {
        "vendor-dir": "api/vendor/auto"
    },
    "require": {
        "slim/slim": "^3.0",
        "monolog/monolog": "^1.22",
        "dflydev/fig-cookies": "^1.0",
        "slim/pdo": "^1.10",
        "akrabat/rka-ip-address-middleware": "^0.5.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^6.0",
        "jakub-onderka/php-parallel-lint": "^0.9.2",
        "phpunit/dbunit": "^3.0"
    },
    "scripts": {
        "test": [
        	"parallel-lint ./api/ --exclude ./api/vendor",
        	"phpunit"
        ]
    }
}
